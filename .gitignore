# Defensive: never commit anything that looks like a secret
.credentials.json
*.credentials.json
*.key
*.pem
.env
.env.*

# OS / editor cruft
.DS_Store
*.swp
*~

# Per-machine night-shift target list (each PC fills in its own repo paths)
scripts/targets.conf

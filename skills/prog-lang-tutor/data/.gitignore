# Per-user/per-machine knowledge banks live here. Don't check them in.
# Each subdirectory <repo-slug>/knowledge.json is generated by
# /prog-lang-tutor analyze and may contain repo-specific code snippets.
*
!.gitignore
!README.md
